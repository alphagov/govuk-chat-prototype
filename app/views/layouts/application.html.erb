<!DOCTYPE html>
<html>
  <head>
    <title>GOV.UK Chat</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link rel="shortcut icon" sizes="16x16 32x32 48x48" href="/assets/favicon.ico" type="image/x-icon">
    <link rel="mask-icon" href="/assets/images/govuk-mask-icon.svg" color="#0b0c0c">
    <link rel="apple-touch-icon" sizes="180x180" href="/assets/images/govuk-apple-touch-icon-180x180.png">
    <link rel="apple-touch-icon" sizes="167x167" href="/assets/images/govuk-apple-touch-icon-167x167.png">
    <link rel="apple-touch-icon" sizes="152x152" href="/assets/images/govuk-apple-touch-icon-152x152.png">
    <link rel="apple-touch-icon" href="/assets/images/govuk-apple-touch-icon.png">
    <meta property="og:image" content="/assets/images/govuk-opengraph-image.png">
  </head>

  <body class="gem-c-layout-for-public govuk-template__body <% unless current_page?(:controller => 'govuk_page') %>govuk-chat<% end %>">
    <script>
      document.body.className = ((document.body.className) ? document.body.className + ' js-enabled' : 'js-enabled');
    </script>

    <a href="#main-content" class="govuk-skip-link <% if !current_page?(:controller => 'govuk_page') %>govuk-skip-link--black<% end %>" data-module="govuk-skip-link">Skip to main content</a>

    <% if current_page?(:controller => 'govuk_page') %>
      <%= render "shared/header", { new_header: false } %>
    <% else %>
      <%= render "shared/header", { new_header: true } %>
    <% end %>

    <div class="govuk-width-container <% unless current_page?(:controller => 'govuk_page') %>govuk-chat-container-wrapper<% end %>">
      <%= yield %>
    </div>

    <script src="../../app.js"></script>
    <script>
      window.GOVUKFrontend.initAll()
    </script>
  </body>
</html>
